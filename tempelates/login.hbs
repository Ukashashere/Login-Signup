<!--Just similar like HTML(it's a comment)-->
<!--Just type '!' and snippet will bring the whole (here,HTML like) format-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">  <!-- 'X-UA compatible' thing automatically came on his computer,  in mine it's not showing like this ,so maybe it dosen't matter -->
    <title>Document</title>
        <link rel="stylesheet" href="/login.css">
</head>
<body>
    <h1>Login Form</h1>

    <form action="/login" method="post" >         <!--'Action' should be same as folder-->
    
        <input placeholder="Name" name="name" type="text">
        <br>
        <input placeholder="Password" name="password" type="password">
        <br>
        <input class="sub" type="submit">
     <h3>Dont have a account ?</h3>
    <a href="/signup">Create a new account</a>
    </form>
    
</body>
</html>


<!--

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">     
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/login.css">
</head>
<body>

    <h1>Login Form</h1>

    <form action="/login" method="post" >
        <input placeholder="Name" name="name" type="text">
        <br>
        <input placeholder="Password" name="password" type="password">
        <br>
        <input class="sub" type="submit">
     <h3>Dont have a account ?</h3>
    <a href="/signup">Create a new account</a>
    </form>


    
</body>
</html>
-->

<!--
This is an HTML document that represents a basic login form for a web application. Let's break down the code:

1. `<!DOCTYPE html>`: This declaration specifies the document type and version of HTML being used (HTML5).

2. `<html lang="en">`: This opening tag indicates the beginning of the HTML document and specifies that the language used is English.

3. `<head>`: This section contains meta-information about the document such as character set, viewport settings, and the title of the document.

   - `<meta charset="UTF-8">`: Specifies the character encoding for the document as UTF-8.
   - `<meta http-equiv="X-UA-Compatible" content="IE=edge">`: Provides instructions to the browser on how to render the webpage. In this case, it tells Internet Explorer to use the latest rendering engine available.
   - `<meta name="viewport" content="width=device-width, initial-scale=1.0">`: Sets the viewport to the width of the device's screen and initial scale to 1.0, which is necessary for responsive design.
   - `<title>Document</title>`: Sets the title of the webpage to "Document".

4. `<link rel="stylesheet" href="css/login.css">`: This line links an external CSS file named "login.css" to style the login form. It's likely used for styling the appearance of the form elements.

5. `<body>`: This section contains the visible content of the webpage.

   - `<h1>Login Form</h1>`: Displays a heading "Login Form" at the top of the page.

   - `<form action="/login" method="post">`: This tag defines a form that allows users to input data. It specifies that when the form is submitted, the data will be sent to the "/login" URL using the HTTP POST method.

     - `<input placeholder="Name" name="name" type="text">`: This input field allows users to input their name. The "placeholder" attribute provides a hint to the user about what to enter, and the "name" attribute specifies the name of the input field when the form is submitted.
     - `<input placeholder="Password" name="password" type="password">`: This input field allows users to input their password. The "type" attribute is set to "password" to obscure the characters entered by the user.
     - `<input class="sub" type="submit">`: This input field represents a submit button that users can click to submit the form. The "class" attribute is set to "sub", which could be used to apply specific styling from the linked CSS file.

   - `<h3>Dont have a account ?</h3>`: Displays a heading "Dont have a account ?" below the form.

   - `<a href="/signup">Create a new account</a>`: This is an anchor element (a link) that points to the "/signup" URL. When clicked, it will redirect users to a page where they can create a new account.

6. `</form>`: Closes the form element.

7. `</body>`: Marks the end of the visible content of the webpage.

8. `</html>`: Marks the end of the HTML document.

-->
